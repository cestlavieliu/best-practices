{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -qE '\\.(js|ts|jsx|tsx|mjs|cjs)$' && npx eslint \"$f\" || true; }",
            "timeout": 30,
            "statusMessage": "Running eslint..."
          }
        ]
      }
    ]
  }
}
